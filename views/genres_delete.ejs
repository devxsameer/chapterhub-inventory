<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head") %>
  <body
    class="h-screen flex flex-col bg-neutral-200 font-outfit overflow-y-auto"
  >
    <%- include("partials/header") %>

    <div class="flex pt-12 h-full">
      <%- include("partials/sidebar") %>

      <div class="xl:ml-64 flex-1">
        <main class="bg-neutral-50">
          <div class="bg-neutral-200 p-4 mb-4 sticky top-12">
            <h2 class="text-3xl font-semibold mb-2">Delete Genre</h2>
            <a href="/genre" class="text-sm underline">Back to Genre</a>
          </div>

          <div class="p-6 max-w-2xl">
            <p class="mb-4">
              Are you sure you want to delete
              <strong><%= genre.name %></strong>?
            </p>

            <form action="" method="POST" class="space-x-3">
              <button class="btn-lg bg-red-800">Yes, Delete</button>
              <a href="/books" class="ml-3 underline text-sm">Cancel</a>
            </form>
          </div>
        </main>

        <%- include("partials/footer") %>
      </div>
    </div>
    <script src="/js/sidebar.js"></script>
  </body>
</html>
